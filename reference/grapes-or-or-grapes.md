# Utility operator for NULL coalescing

Utility operator for NULL coalescing

## Usage

``` r
x %||% y
```

## Arguments

- x:

  First value.

- y:

  Second value (used if x is `NULL`).

## Value

`x` if not `NULL`, otherwise `y`.
