% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retry_download.R
\name{.has_internet_curl}
\alias{.has_internet_curl}
\title{read.abares download utilities (httr2-based, internal)}
\usage{
.has_internet_curl()
}
\value{
Logical(1), \code{TRUE} if internet appears available.
}
\description{
Internal utilities to download files with \code{httr2}, dataset-driven timeouts,
auto streaming selection, resume for interrupted downloads, and safe cleanup
of partial files. Internet availability is checked with \code{curl::has_internet()}.
All functions here are internal; do not export.
}
\section{Options respected}{

\itemize{
\item \code{read.abares.verbosity}: character; if \code{"verbose"} a progress bar is shown.
\item \code{read.abares.max_tries}: integer; maximum retry attempts (default \code{3}).
\item \code{read.abares.dataset_timeouts}: named list mapping \code{dataset_id} to timeout
lists with fields \code{connect}, \code{total}, \code{low_speed_time}, \code{low_speed_limit}.
\item \code{read.abares.user_agent}: character; custom User-Agent string.
\item Global timeout fallbacks:
\itemize{
\item \code{read.abares.timeout_connect} (default \code{15} seconds),
\item \code{read.abares.timeout_total} (default \code{7200} seconds),
\item \code{read.abares.timeout} (legacy alias for total),
\item \code{read.abares.low_speed_time} (default \code{0} seconds),
\item \code{read.abares.low_speed_limit} (default \code{0} bytes/sec).
}
\item \code{read.abares.stream_threshold_mb}: numeric; auto-stream threshold (default 50).
}

Check internet connectivity using curl (internal)
}

\keyword{internal}
