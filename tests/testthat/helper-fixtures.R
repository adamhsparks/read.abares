locate_agfd_fixture <- function(name = "agfd_fixture_valid_nc.zip") {
  system.file("extdata", name, package = "read.abares")
}

locate_clum_fixture <- function(name = "agfd_fixture_valid_nc.zip") {
  system.file("extdata", name, package = "read.abares")
}
